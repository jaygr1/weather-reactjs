var React = require('react');
var Home = rquire('../components/Home');

var HomeContainer = React.createClass({
  render: function() {
    return (
      <Home/>
    )
  }
});

module.exports = HomeContainer;
