var React = require('react');
var GetLocation = require('../components/getLocation');
var PropTypes = React.PropTypes;

var GetLocationContainer = React.createClass({
  render: function () {
    return (
      <GetLocation />
    )
  }
});

module.exports = GetLocationContainer;
